# 配电网本体模型构建与知识推理 · 设计文档

## 一、本体与数据库的本质区别

数据库与本体回答两类不同的问题。数据库存储"事实"，只能回答**已经显式存在**的查询——"配变 A 挂在馈线 X 下"。而"馈线 X 的 S1 断开 → 下游配变 A/B/C 失电 → 影响 1200 户 → 应合上联络开关 L1 转供到馈线 Y"这条完整推理链，**不存在于任何表中**，是沿拓扑逐跳推导出的新知识。一句话：**数据库回答"是什么"，本体回答"如果……那么……"**。

本体由类（概念体系）、属性（语义关系）、规则（推理公理）三部分组成，是"知识的骨架"——数据库是骨头上的肉，本体定义了骨头的结构，使 AI 不仅能检索，还能推理。

## 二、设计取舍

**1. 建模粒度**：参考国际标准 IEC 61970/61968 CIM 的类层次（PowerSystemResource → Equipment → ConductingEquipment → Switch），但做了精简——只保留配网推理必需的概念（变电站、馈线、开关、配变、线路段、用户、故障、工单等 19 个类），舍弃 CIM 中面向发电、输电的庞大体系。取舍标准是"够用且自洽"：粒度到配变/用户组，不到电表级，因为停电影响、过载、转供均在该粒度可判定。

**2. 表示语言**：本体用 YAML 自定义 DSL 而非 OWL XML。原因：①YAML 易读易维护，非本体专家也能评审；②可直接被 Python 解析并与 LLM 提示词无缝拼接；③规则采用"YAML 声明意图 + Python 实现逻辑"的双层结构，每条规则附 SWRL 风格形式化表达，兼顾规范性与可执行性，可后续导出 OWL。

**3. 推理方式**：确定性规则引擎（拓扑遍历 + 阈值判定），而非 LLM 直接推理或标准推理机（Pellet/HermiT）。拓扑传播是精确计算，LLM 推算会出错且不可溯源；自研引擎轻量可控，能与 Agent 架构深度集成。**这是"符号推理 + LLM"混合架构的刻意选择**——LLM 负责解析意图，引擎负责执行推理，答案可 100% 溯源。

**4. 规则设计**：9 条规则覆盖四类问题——拓扑类（R1 故障传播、R9 多断点合并）、归因类（R2 停电影响、R3 户数聚合）、阈值类（R4/R5 过载触发）、决策类（R6 转移可行性、R7 转移后校验、R8 检修隔离）。其中 R6/R7 是核心：转移可行性 = 联络开关接入 + 对侧馈线剩余容量 ≥ 转移负荷，转移后校验防止"救了 A 馈线、烧了 B 馈线"的次生风险。

## 三、本体如何支持推理

推理链路分三步。**结构支撑**：`hasDownstream/hasUpstream` 对象属性把电网建成有向图，`serves` 关联配变与用户，`connectsTo` 记录跨馈线联络开关——拓扑关系是全部推理的地基。**规则执行**：R1 从断开的开关沿下游 BFS 传播失电（遇断开的开关停止），R4/R5 对超阈值的设备触发过载告警，R6/R7 对失电/过载区域评估经联络开关转供对侧馈线的可行性。**结果聚合**：R2/R3 将失电配变映射为用户组并求和户数，R8 反向沿上游为检修设备定位隔离开关并防止倒送电。

本体还带来**可迁移性**：新增馈线只需补充实例数据，推理能力自动覆盖；新增规则只需在 YAML 声明并实现对应函数。这正是本体相对硬编码规则的价值——**知识（本体+规则）与计算（引擎）分离**，知识可被非工程师评审与扩展。
