# 配电网本体知识推理项目 · 测试报告

测试时间：2026-08-27 · 测试环境：conda `dianwang`（Python 3.10.20，`D:\conda25\envs\dianwang`）
测试范围：推理引擎（单元/边界）、API 接口（全量）、Demo 页面

---

## 一、测试结果总览

| 测试套件 | 用例数 | 通过 | 结论 |
|---|---|---|---|
| `verification.py`（题目 5 问） | 13 断言 | 13 | ✅ 全部通过 |
| `test_suite.py`（边界场景） | 14 断言 | 14 | ✅ 全部通过 |
| API 接口测试（curl） | 14 项 | 14 | ✅ 全部通过 |
| **合计** | **41** | **41** | ✅ **全部通过** |

## 二、推理引擎测试明细（test_suite.py）

| 用例 | 场景 | 预期 | 结果 |
|---|---|---|---|
| T1 | 初始状态全网正常（L1 常开） | 无失电 | ✅ |
| T2 | 多断点合并（S1+S3 断开，规则 R9） | 失电配变去重，1700 户 | ✅ |
| T3 | 合环转供（S2 断 + L1 合） | 后援供电，无失电 | ✅ |
| T4 | L1 单独合闸 | 无失电 | ✅ |
| T5 | 临时断点不改变持久状态 | S1 仍闭合 | ✅ |
| T6 | 转移判定：T1 过载可转移 / 无配变区域拒绝 / 未知联络开关拒绝 | 三种结果正确 | ✅ |
| T7 | 检修隔离：已知工单隔离 S2+L1 / 未知工单报错 | 正确 | ✅ |
| T8 | 过载巡检：仅 T1 过载 88.9% | 正确 | ✅ |
| T9 | 断路器/分段开关初始全部闭合 | 正确 | ✅ |

## 三、API 接口测试明细

| 接口 | 测试内容 | 结果 |
|---|---|---|
| `GET /` | 首页 200，标题正确 | ✅ |
| `GET /api/topology` | 17 节点 / 15 边 / summary 正常 | ✅ |
| `GET /api/ontology` | 19 类 / 10 对象属性 / 9 规则 | ✅ |
| `POST /api/ask` | 5 类意图（outage/overload/transfer/isolation/status）+ 拒答 | ✅ |
| `POST /api/set_switch` | 合法开关切换生效；非法设备返回错误 | ✅ |
| `POST /api/reset` | 重置后全网恢复正常 | ✅ |
| `GET /api/ask_stream` | SSE 流式 6 帧推理链路 | ✅ |

交互场景实测：断开 S1 → 6 个设备失电；S2 断开 + 合上 L1 → 合环转供成功（失电清零）；S2 断开（L1 常开）→ LsegX/T1/T2 失电。

## 四、测试中发现并修复的 Bug（4 个）

| # | Bug | 影响 | 修复 |
|---|---|---|---|
| 1 | 母线实例缺少 `downstream` 属性，带电 BFS 从母线找不到电源出口 | 失电计算错误（全员失电） | `ontology.yaml` 为 Bus1 补充 `downstream: [CB1, CB2]` |
| 2 | 联络开关传播逻辑跳过 L1 自身节点（LsegB 直连 LsegX） | L1 合闸时被误判为失电 | `_neighbors` 让 L1 作为中间节点参与 BFS |
| 3 | `compute_outage` 排除断开开关时读取恢复后的状态 | 临时断点（S1）被误算进失电集合 | 记录 `open_set` 一并排除 |
| 4 | `/api/reset` 只恢复内存状态，未同步 `graph.devices` | 重置后拓扑仍显示失电 | 保存初始状态快照，重置时双端同步 |

## 五、结论

- 题目 5 问验证（S1 断 1000 户 / T1 过载可转 / CB1 断 1100 户 / 检修隔离 / 转供拒绝）全部可手算复核；
- 引擎现已正确处理**合环转供**（闭合联络开关提供后援供电）、**多断点合并**、**状态隔离**等真实电网场景，理论正确性完整；
- 全部 41 项断言通过，Demo 服务运行正常（http://localhost:8000）。
