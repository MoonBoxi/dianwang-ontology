# 配电网本体模型构建与知识推理 · 交付说明

> 致：项目评审 / HR
> 说明：本包为本次题目完整交付物，包含题目 5 项要求全部成果、可运行的 Demo 系统、自动化验证结果与设计文档。Demo 已实机运行验证，可预约线上演示。

---

## 一、题目要求 × 交付成果对照

| 题目要求 | 交付内容 | 位置 |
|---|---|---|
| ① 本体模型设计（≥15 类、继承、属性、≥8 规则） | **19 个类**（4 层继承）/ **32 数据属性 / 23 对象属性** / **9 条推理规则**（含 SWRL 风格公理） | `ontology.yaml` |
| ② 推理验证（5 个推理问题 + 预期答案） | 5 问 **13 项断言全部通过**（`python verification.py`） | `verification.py` |
| ③ 本体可视化 | 类继承树 + 对象属性 + 规则 + 实时拓扑图（Demo 内"本体模型"页） | `static/index.html` |
| ④ 设计文档（800 字） | 本体 vs 数据库本质区别 / 设计取舍 / 推理支撑机制（约 860 字） | `design_doc.md` |
| ⑤ Demo：本体可视化 + Agent 对话推理 | 在线 Demo：拓扑可视化 + 点击开关实时推理 + LangGraph Agent 对话 | `app.py` + `agent_graph.py` |
| ⑥ 与模型交互记录 | **10 轮真实会话实录**（提问 → 意图识别 → 推理链路 → 系统回答 → 交互思路） | `人机交互记录.md` |

## 二、5 个推理问题验证结果（可手算复核）

| 问题 | 推理规则 | 验证结果 |
|---|---|---|
| Q1 分段开关 S1 断开后影响哪些用户？ | R1→R2→R3 | 配变 T1/T2/T3 失电，**1000 户**停电 ✅ |
| Q2 配变 T1 过载（88.9%），能否经联络开关 L1 转馈线 F2？ | R4→R6→R7 | 可行，合闸后 F2 负载率 **81.9%** ✅ |
| Q3 馈线 F1 首端断路器 CB1 跳闸影响多少用户？ | R1→R2→R3 | **1100 户**（含配变 T0） ✅ |
| Q4 检修配变 T1 需要断开哪些开关？ | R8 | 断开 S2，保持联络开关 L1 断开（防倒送电） ✅ |
| Q5 分段开关 S2 断开后合上 L1 能否恢复供电？ | R6→R7 | 拒绝：F2 将过载至 **100.6%**（避免次生事故） ✅ |

**自动化回归：27/27 断言全部通过**（`verification.py` 13 项 + `test_suite.py` 14 项边界场景）。

## 三、Demo 系统能力

- **拓扑可视化**：变电站/馈线/分段开关/联络开关/配变/用户分层展示，馈线分色（F1 蓝 / F2 青），点击任意开关节点切换合分，**失电设备自动高亮**（灰底红框发光）、过载设备红色告警
- **Agent 对话推理**（LangGraph 状态机编排）：支持自然语言提问与数字快捷指令（输入 1-5），回答附**完整推理链路溯源**（规则编号 + 步骤），支持 SSE 流式输出
- **合环转供**：S2 断开后合上联络开关 L1，失电区域由对侧馈线后援供电自动恢复（带电可达性分析）
- **Neo4j 图数据库验证**：本体实例已导入 Neo4j，Cypher 可变长路径查询结果与规则引擎**完全一致**（`/api/neo4j?sw=S1`）
- **混合架构**：确定性规则引擎负责推理（100% 可溯源、可手算复核），LLM 仅做意图解析（未配置 key 自动降级，可离线运行）

## 四、技术架构

```
用户提问 ──► LangGraph Agent（意图解析：规则/可选 LLM）
                 │ 条件路由（StateGraph）
                 ├─ outage / transfer / overload / isolation / status 节点
                 └─ 每个节点调用推理引擎（确定性规则 R1~R9）
前端(ECharts) ◄── /api/topology（失电/过载高亮）
对话界面 ◄──── /api/ask（答案 + 推理链路）
```

技术栈：Python 3.10 · FastAPI · LangGraph · PyYAML · ECharts · Neo4j（可选）· Docker（可选）

## 五、运行方式（若需本地复现）

```bash
# 方式一：conda 环境（已配置 dianwang 环境）
conda activate dianwang
python verification.py          # 推理验证
uvicorn app:app --port 8000     # 启动 Demo → http://localhost:8000

# 方式二：Docker 一键启动（含 Neo4j）
docker compose -p dianwang up -d --build
```

## 六、文件清单

```
ontology.yaml          本体模型（19 类 / 55 属性 / 9 规则 / 实例电网）
inference_engine.py    推理引擎（故障传播/过载/负荷转移/检修隔离）
verification.py        5 问验证（13 断言）
test_suite.py          边界测试（14 断言：多断点/合环转供/状态隔离）
agent_graph.py         LangGraph Agent 编排
app.py                 FastAPI 后端（拓扑/对话/SSE/Neo4j 接口）
static/index.html      前端（拓扑可视化 + 对话 + 本体模型页）
design_doc.md          800 字设计文档
neo4j_import.py        Neo4j 数据导入与一致性校验
Dockerfile / docker-compose.yml   容器化部署
test_report.md         测试报告
```
